---
// src/components/Header.astro
---
<header class="fixed top-0 w-full z-50 bg-black/60 backdrop-blur-xl border-b border-gray-900">
  <div class="max-w-7xl mx-auto px-6 h-20 flex items-center justify-between">
    <a id="logo-link" href="/" class="text-2xl font-black text-white tracking-tighter uppercase italic">
      Gym<span class="text-red-600">App</span>
    </a>

    <div id="auth-zone">
      <a id="login-btn" href="/login" class="hidden text-xs font-bold text-white bg-red-600 px-6 py-2.5 rounded-full uppercase tracking-widest hover:bg-red-700 transition-all">
        Iniciar Sesión
      </a>

      <div id="user-menu" class="hidden relative group">
        <button class="flex items-center gap-3 bg-gray-900/50 px-4 py-2 rounded-full border border-gray-800 group-hover:border-red-600 transition-all">
          <div class="w-7 h-7 bg-red-600 rounded-full flex items-center justify-center text-[10px] font-black text-white uppercase" id="user-initials">--</div>
          <span class="text-xs font-bold text-white uppercase tracking-tight" id="user-name-display">Cargando...</span>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-gray-500 group-hover:text-white transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
          </svg>
        </button>
        
        <div class="absolute right-0 mt-2 w-56 bg-black border border-gray-800 rounded-2xl shadow-2xl opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-300 overflow-hidden z-[100]">
          <div class="p-4 bg-gray-900/20">
            <p class="text-[10px] text-gray-500 uppercase font-bold mb-1 tracking-widest">Atleta</p>
            <p class="text-xs text-white font-bold truncate" id="dropdown-email">---</p>
          </div>

          <div class="border-t border-gray-900 bg-red-950/5">
            <button id="logout-btn" class="w-full text-left px-4 py-4 text-[11px] text-red-500 hover:bg-red-600 hover:text-white transition-all font-black uppercase tracking-[0.2em]">
              Cerrar Sesión
            </button>
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<script>
  function updateHeader() {
    const token = localStorage.getItem('token');
    const userString = localStorage.getItem('user');
    
    const userMenu = document.getElementById('user-menu');
    const loginBtn = document.getElementById('login-btn');
    const logoLink = document.getElementById('logo-link') as HTMLAnchorElement;

    // Lógica del Logo e Interfaz
    if (token && userString && userString !== 'undefined') {
      const user = JSON.parse(userString);
      
      // Si hay sesión, el logo lleva al Dashboard
      if (logoLink) logoLink.href = "/dashboard";
      
      userMenu?.classList.remove('hidden');
      loginBtn?.classList.add('hidden');
      
      const nameDisplay = document.getElementById('user-name-display');
      const initialsDisplay = document.getElementById('user-initials');
      const dropdownEmail = document.getElementById('dropdown-email');

      if (nameDisplay) nameDisplay.textContent = user.name;
      if (initialsDisplay) initialsDisplay.textContent = user.name.substring(0, 2).toUpperCase();
      if (dropdownEmail) dropdownEmail.textContent = user.email;
    } else {
      // Si NO hay sesión, el logo lleva al Index
      if (logoLink) logoLink.href = "/";
      
      userMenu?.classList.add('hidden');
      loginBtn?.classList.remove('hidden');
    }
  }

  updateHeader();

  document.getElementById('logout-btn')?.addEventListener('click', () => {
    localStorage.removeItem('token');
    localStorage.removeItem('user');
    window.location.href = '/';
  });
</script>