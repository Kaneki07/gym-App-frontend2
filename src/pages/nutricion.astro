---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Mi Nutrición | Gym App">
  <div class="max-w-7xl mx-auto px-6 py-12">
    <header class="mb-12">
      <h1 class="text-4xl md:text-6xl font-black uppercase tracking-tighter text-white italic">
        Tu Plan de <span class="text-red-600 drop-shadow-[0_0_10px_rgba(220,38,38,0.5)]">Alimentación</span>
      </h1>
      <p class="text-gray-500 mt-4 max-w-2xl uppercase font-bold tracking-widest text-xs">
        Consulta las dietas personalizadas asignadas por tu entrenador para optimizar tu rendimiento.
      </p>
    </header>

    <div id="dietas-container" class="grid grid-cols-1 md:grid-cols-2 gap-8">
      <div class="col-span-full py-20 text-center">
        <div class="inline-block animate-spin rounded-full h-8 w-8 border-t-2 border-red-600 mb-4"></div>
        <p class="text-gray-500 font-black uppercase tracking-widest text-xs">Sincronizando con el servidor...</p>
      </div>
    </div>

    <div class="mt-12">
        <a href="/dashboard" class="inline-flex items-center gap-2 text-gray-500 hover:text-white transition-colors font-black uppercase tracking-widest text-[10px]">
            <span class="text-lg">←</span> Volver al panel
        </a>
    </div>
  </div>
</Layout>

<script>
  async function fetchDietas() {
    const container = document.getElementById('dietas-container');
    const userString = localStorage.getItem('user');
    const user = JSON.parse(userString || '{}');

    if (!user.id) {
        if (container) container.innerHTML = `<p class="text-red-500 uppercase font-black">Error: Usuario no identificado</p>`;
        return;
    }

    try {
      const response = await fetch(`https://gym-app-backend-2-pv91.onrender.com/dietas/user/${user.id}`);
      const result = await response.json();

      if (!container) return;

      if (result.code === 200 && result.data.length > 0) {
        container.innerHTML = result.data.map((dieta: any) => `
          <div class="group bg-gray-900/40 border border-red-600/20 p-8 rounded-[2.5rem] shadow-[0_20px_40px_rgba(0,0,0,0.4)] hover:border-red-600/60 transition-all duration-500">
            <div class="flex justify-between items-start mb-6">
                <div class="w-12 h-12 bg-red-900/20 border border-red-600/30 rounded-xl flex items-center justify-center group-hover:bg-red-600 transition-all duration-500">
                    <span class="text-2xl">🥗</span>
                </div>
                <span class="text-[10px] font-black text-gray-600 uppercase tracking-[0.2em]">
                    ${new Date(dieta.createdAt).toLocaleDateString()}
                </span>
            </div>
            
            <h3 class="text-2xl font-black text-white mb-4 uppercase italic tracking-tighter">
                ${dieta.title}
            </h3>
            
            <div class="text-gray-400 text-sm leading-relaxed space-y-4 whitespace-pre-wrap border-l-2 border-red-600/30 pl-4 py-2">${dieta.content.trim()}</div>

            <div class="mt-8 flex items-center text-[9px] font-black text-red-600 uppercase tracking-[0.3em]">
                Plan verificado por sistema <span class="ml-2">✓</span>
            </div>
          </div>
        `).join('');
      } else {
        container.innerHTML = `
          <div class="col-span-full bg-gray-900/20 border border-dashed border-gray-800 p-20 rounded-[2.5rem] text-center">
            <p class="text-gray-600 font-black uppercase tracking-widest text-sm">Aún no tienes dietas asignadas</p>
            <p class="text-gray-700 text-xs mt-2">Contacta con tu coach para recibir tu plan nutricional.</p>
          </div>
        `;
      }
    } catch (error) {
      if (container) {
        container.innerHTML = `<p class="text-red-500 uppercase font-black">Error al conectar con el servidor</p>`;
      }
    }
  }

  fetchDietas();
</script>

<style>
  div::-webkit-scrollbar {
    width: 4px;
  }
  div::-webkit-scrollbar-track {
    background: transparent;
  }
  div::-webkit-scrollbar-thumb {
    background: #dc2626;
    border-radius: 10px;
  }
</style>