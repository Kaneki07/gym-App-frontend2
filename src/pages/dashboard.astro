---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Panel de Control | Gym App">
  <div class="fixed inset-0 z-0 pointer-events-none bg-[url('/grid-pattern.png')] opacity-[0.03] mix-blend-overlay"></div>

  <div class="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 py-8">
    
    <header class="mb-10 p-6 rounded-3xl bg-zinc-900/60 border border-zinc-800/50 backdrop-blur-md shadow-xl relative overflow-hidden">
       <div class="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-red-600/0 via-red-600/50 to-red-600/0"></div>
      
      <div class="flex flex-col md:flex-row md:items-end justify-between gap-6 relative z-10">
        <div>
          <h1 class="text-3xl md:text-5xl font-black uppercase italic tracking-tighter text-white">
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-white to-gray-500">Bienvenido,</span>
            <br class="md:hidden">
            <span class="text-red-600 drop-shadow-[0_0_15px_rgba(220,38,38,0.8)]" id="welcome-name">Atleta</span>
          </h1>
        </div>
        
        <div class="relative inline-block text-left" id="status-menu-container">
          <p class="text-[9px] text-red-400/80 font-black uppercase tracking-[0.2em] mb-2 text-right">Estado del Sistema</p>
          <button 
            id="open-status-menu"
            class="group flex items-center justify-between gap-4 bg-black hover:bg-zinc-900 text-white pl-4 pr-3 py-3 rounded-xl border-l-4 border-red-600 shadow-[0_5px_15px_-5px_rgba(0,0,0,1)] active:scale-95 transition-all min-w-[200px]"
          >
            <div class="flex flex-col items-start">
                <span id="current-status" class="font-black italic uppercase text-sm tracking-tight">Cargando...</span>
                <span class="text-[9px] text-gray-500 font-medium leading-none mt-1">Click para cambiar</span>
            </div>
            <div class="w-8 h-8 bg-red-600/20 rounded-lg flex items-center justify-center group-hover:bg-red-600 transition-colors">
                 <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4 text-red-500 group-hover:text-white transition-colors" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
                </svg>
            </div>
          </button>

          <div 
            id="status-dropdown" 
            class="hidden absolute right-0 mt-2 w-64 bg-black/95 border border-red-600/30 rounded-xl shadow-[0_0_30px_rgba(220,38,38,0.15)] z-50 overflow-hidden backdrop-blur-xl"
          >
            <button class="status-option w-full text-left px-5 py-4 text-xs font-black uppercase tracking-wider text-gray-400 hover:bg-red-600 hover:text-white transition-all border-b border-zinc-900 flex items-center justify-between group" data-status="Mantenimiento">
              <span>Mantenimiento</span>
              <span class="opacity-0 group-hover:opacity-100 transition-opacity">⚡</span>
            </button>
            <button class="status-option w-full text-left px-5 py-4 text-xs font-black uppercase tracking-wider text-gray-400 hover:bg-red-600 hover:text-white transition-all border-b border-zinc-900 flex items-center justify-between group" data-status="Fase de Definición">
              <span>Fase de Definición</span>
              <span class="opacity-0 group-hover:opacity-100 transition-opacity">🔥</span>
            </button>
            <button class="status-option w-full text-left px-5 py-4 text-xs font-black uppercase tracking-wider text-gray-400 hover:bg-red-600 hover:text-white transition-all flex items-center justify-between group" data-status="Fase de Volumen">
              <span>Fase de Volumen</span>
              <span class="opacity-0 group-hover:opacity-100 transition-opacity">🦍</span>
            </button>
          </div>
        </div>
      </div>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-5 auto-rows-fr">
      
      <div id="admin-panel-card" class="hidden md:col-span-2 lg:col-span-3 relative overflow-hidden rounded-2xl bg-gradient-to-r from-black to-zinc-900 border-l-[6px] border-red-600 shadow-2xl hover:shadow-red-600/20 transition-all duration-300 group hover:-translate-y-1">
        <div class="absolute -right-10 -bottom-10 text-[150px] opacity-5 select-none group-hover:scale-110 transition-transform duration-500">🛡️</div>
        <div class="p-8 relative z-10 h-full flex flex-col md:flex-row items-start md:items-center justify-between gap-8">
            <div class="flex-1">
                <div class="flex items-center gap-4 mb-2">
                    <span class="text-3xl drop-shadow-[0_0_10px_rgba(220,38,38,0.8)]">🛡️</span>
                    <h3 class="text-2xl font-black text-white uppercase italic tracking-tighter">Centro de Comando Coach</h3>
                </div>
                <p class="text-gray-400 text-sm font-medium max-w-lg">Acceso prioritario a herramientas de gestión de atletas y programación.</p>
            </div>
            <div class="flex flex-wrap gap-3 w-full md:w-auto">
                <a href="/dietasAdmin" class="px-6 py-3 bg-red-600/10 hover:bg-red-600 text-red-500 hover:text-white border border-red-600/30 rounded-lg text-xs font-black uppercase tracking-widest transition-all flex-1 md:flex-none text-center">
                    Subir Dietas 🍎
                </a>
                <a href="/rutinasAdmin" class="px-6 py-3 bg-red-600/10 hover:bg-red-600 text-red-500 hover:text-white border border-red-600/30 rounded-lg text-xs font-black uppercase tracking-widest transition-all flex-1 md:flex-none text-center">
                    Subir Rutinas 💪
                </a>
            </div>
        </div>
      </div>

      <a href="/perfilCoach" id="coach-profile-card" class="hidden relative overflow-hidden rounded-2xl bg-gradient-to-br from-black to-zinc-900 border-l-[6px] border-yellow-500 shadow-lg hover:shadow-yellow-500/20 transition-all duration-300 group hover:-translate-y-1">
        <div class="absolute -right-6 -bottom-6 text-9xl opacity-5 select-none group-hover:rotate-12 transition-transform duration-500 text-yellow-500">🆔</div>
        <div class="p-6 relative z-10 h-full flex flex-col">
            <div class="flex justify-between items-start mb-4">
                <h3 class="text-xl font-black text-white uppercase italic tracking-tighter leading-none">Mi Perfil <br>Pro</h3>
                <div class="w-10 h-10 rounded-lg bg-yellow-500/10 border border-yellow-500/30 flex items-center justify-center text-xl text-yellow-500 group-hover:bg-yellow-500 group-hover:text-black transition-all">
                    🆔
                </div>
            </div>
            <p class="text-gray-500 text-xs font-medium mb-8 leading-relaxed">Gestiona tu imagen pública visible para clientes.</p>
            <div class="mt-auto flex justify-end">
                 <span class="text-[10px] font-black text-yellow-500 uppercase tracking-widest group-hover:underline">Acceder interfaz →</span>
            </div>
        </div>
      </a>

       {[
        { href: "/rutinaPersonal", title: "Plan Técnico", icon: "⚡", desc: "Tu programación personalizada." },
        { href: "/rutinas", title: "Explorar Rutinas", icon: "💪", desc: "Entrenamientos de élite prediseñados." },
        { href: "/nutricion", title: "Nutrición", icon: "🍎", desc: "Combustible de precisión." },
        { href: "/medidas", title: "Medidas", icon: "📏", desc: "Evolución de circunferencias." },
        { href: "/calculadora", title: "Biometría", icon: "📊", desc: "Monitoreo de porcentaje graso." },
        { href: "/bitacora", title: "Bitácora Pro", icon: "📝", desc: "Registro diario de entrenamiento." },
        // Añade esto al inicio del array de cards en dashboard.astro
        { href: "/iaFit", title: "IA Fit Scan", icon: "🤖", desc: "Analiza tus macros con solo una foto." },
        
      ].map(card => (
        <a href={card.href} class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-black to-zinc-900 border-l-[6px] border-red-600/80 shadow-lg hover:shadow-red-600/30 transition-all duration-300 group hover:-translate-y-1">
            {/* Icono fantasma de fondo */}
            <div class="absolute -right-6 -bottom-6 text-9xl opacity-[0.07] select-none group-hover:scale-110 group-hover:-rotate-12 transition-transform duration-500 text-red-600">{card.icon}</div>
            
            <div class="p-6 relative z-10 h-full flex flex-col">
                <div class="flex justify-between items-start mb-4">
                    <h3 class="text-xl font-black text-white uppercase italic tracking-tighter leading-none min-h-[2.5rem] flex items-center">
                        {card.title}
                    </h3>
                    {/* Icono indicador superior derecho */}
                    <div class="w-10 h-10 rounded-lg bg-red-600/10 border border-red-600/30 flex items-center justify-center text-xl group-hover:bg-red-600 group-hover:text-white transition-all shadow-[0_0_10px_rgba(220,38,38,0.2)]">
                        {card.icon}
                    </div>
                </div>
                <p class="text-gray-500 text-xs font-medium mb-6 leading-relaxed">{card.desc}</p>
                
                <div class="mt-auto flex justify-between items-center opacity-60 group-hover:opacity-100 transition-opacity">
                    <div class="h-1 w-12 bg-red-600/30 overflow-hidden rounded-full">
                        <div class="h-full w-full bg-red-600 -translate-x-full group-hover:translate-x-0 transition-transform duration-500 ease-out"></div>
                    </div>
                    <span class="text-[9px] font-black text-red-500 uppercase tracking-widest">Activar →</span>
                </div>
            </div>
        </a>
      ))}

      <a href="/coaches" class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-black to-zinc-900 border-l-[6px] border-blue-600 shadow-lg hover:shadow-blue-600/20 transition-all duration-300 group hover:-translate-y-1">
        <div class="absolute -right-6 -bottom-6 text-9xl opacity-5 select-none group-hover:scale-105 transition-transform duration-500 text-blue-500">🏆</div>
        <div class="p-6 relative z-10 h-full flex flex-col">
            <div class="flex justify-between items-start mb-4">
                <h3 class="text-xl font-black text-white uppercase italic tracking-tighter leading-none">Staff de <br>Coaches</h3>
                <div class="w-10 h-10 rounded-lg bg-blue-600/10 border border-blue-600/30 flex items-center justify-center text-xl text-blue-500 group-hover:bg-blue-600 group-hover:text-white transition-all">
                    🏆
                </div>
            </div>
            <p class="text-gray-500 text-xs font-medium mb-8 leading-relaxed">Encuentra al mentor perfecto para tu transformación.</p>
            <div class="mt-auto flex justify-end">
                 <span class="text-[10px] font-black text-blue-500 uppercase tracking-widest group-hover:underline">Explorar Staff →</span>
            </div>
        </div>
      </a>

    </div>
  </div>
</Layout>

<script>
  // --- LÓGICA DEL DASHBOARD (Sin cambios funcionales) ---
  const token = localStorage.getItem('token');
  
  if (!token) {
    window.location.href = '/login';
  } else {
    const userStr = localStorage.getItem('user');
    let user = { id: 'guest', name: 'Atleta', role: 'user' }; 
    
    try {
        if (userStr) user = JSON.parse(userStr);
    } catch (e) {
        console.error("Error al leer datos del usuario", e);
    }

    // --- LÓGICA DE ROLES PARA COACH ---
    if (user.role && user.role.toLowerCase() === 'coach') {
        // Usamos display grid/flex para mostrar en lugar de quitar 'hidden' directamente
        // para mantener el layout del grid
        const adminPanel = document.getElementById('admin-panel-card');
        if (adminPanel) {
            adminPanel.classList.remove('hidden');
            adminPanel.classList.add('flex');
        }
        const profileCard = document.getElementById('coach-profile-card');
        if (profileCard) profileCard.classList.remove('hidden');
    }

    const welcomeName = document.getElementById('welcome-name');
    if (user.name && welcomeName) {
      welcomeName.textContent = user.name.split(' ')[0];
    }

    // --- ESTADO DEL USUARIO ---
    const statusDisplay = document.getElementById('current-status');
    const menuBtn = document.getElementById('open-status-menu');
    const dropdown = document.getElementById('status-dropdown');
    const options = document.querySelectorAll('.status-option');

    menuBtn?.addEventListener('click', (e) => {
      e.stopPropagation();
      dropdown?.classList.toggle('hidden');
    });

    document.addEventListener('click', () => {
      dropdown?.classList.add('hidden');
    });

    function setStatus(newStatus: string) {
      if (statusDisplay) {
          statusDisplay.textContent = newStatus;
          localStorage.setItem(`status_${user.id}`, newStatus);
      }
      dropdown?.classList.add('hidden');
    }

    const savedStatus = localStorage.getItem(`status_${user.id}`) || 'Fase de Volumen';
    if (statusDisplay) statusDisplay.textContent = savedStatus;

    options.forEach(option => {
      option.addEventListener('click', () => {
          const val = (option as HTMLElement).dataset.status;
          if (val) setStatus(val);
      });
    });
  }
</script>